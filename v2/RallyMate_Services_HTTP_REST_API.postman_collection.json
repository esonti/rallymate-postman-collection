{
	"info": {
		"_postman_id": "rallymate-services-http-v2",
		"name": "RallyMate Services - HTTP REST API v2",
		"description": "Comprehensive HTTP REST API collection for RallyMate Services with gRPC-Gateway support.\n\nThis collection covers all services:\n- Authentication (OTP, OTC, Sessions)\n- Users (Profile, Memberships, Discovery)\n- Facilities (CRUD operations)\n- Bridge (Registration, Edge Connections, Tunnels)\n- Locks (Registration, Control, Activities)\n- Cameras (Registration, Control, Activities)\n- Videos (Upload, Association, Management)\n- System Support (Admin roles)\n\nFeatures:\n- Realistic test data with examples\n- Automated test scripts for data extraction\n- Response validation\n- Variable management for chained requests\n- Authentication token handling",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
		"_exporter_id": "rallymate-v2"
	},
	"auth": {
		"type": "bearer",
		"bearer": [
			{
				"key": "token",
				"value": "{{session_token}}",
				"type": "string"
			}
		]
	},
	"event": [
		{
			"listen": "prerequest",
			"script": {
				"type": "text/javascript",
				"exec": [
					"// Global pre-request script",
					"// Set base URL if not already set",
					"if (!pm.variables.get('base_url')) {",
					"    pm.variables.set('base_url', pm.environment.get('base_url') || 'http://localhost:8080');",
					"}",
					"",
					"// Log request details for debugging",
					"console.log('Request: ' + pm.request.method + ' ' + pm.request.url);",
					"if (pm.request.body && pm.request.body.raw) {",
					"    console.log('Body: ' + pm.request.body.raw);",
					"}"
				]
			}
		},
		{
			"listen": "test",
			"script": {
				"type": "text/javascript",
				"exec": [
					"// Global test script",
					"// Log response for debugging",
					"console.log('Response Status: ' + pm.response.code);",
					"console.log('Response Time: ' + pm.response.responseTime + 'ms');",
					"",
					"// Validate response time",
					"pm.test('Response time is acceptable', function() {",
					"    pm.expect(pm.response.responseTime).to.be.below(5000);",
					"});"
				]
			}
		}
	],
	"variable": [
		{
			"key": "base_url",
			"value": "http://localhost:8080",
			"type": "string"
		},
		{
			"key": "session_token",
			"value": "",
			"type": "string"
		},
		{
			"key": "refresh_token",
			"value": "",
			"type": "string"
		},
		{
			"key": "user_id",
			"value": "",
			"type": "string"
		},
		{
			"key": "phone_number",
			"value": "+1234567890",
			"type": "string"
		},
		{
			"key": "facility_id",
			"value": "1",
			"type": "string"
		},
		{
			"key": "device_id",
			"value": "bridge-001",
			"type": "string"
		}
	],
	"item": []
}
